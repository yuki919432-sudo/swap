# Expo/React Native + pnpm: the RN/Metro toolchain resolves some packages
# (metro-runtime, etc.) by walking a flat node_modules, which pnpm's default
# isolated linker does not provide. Hoisted linking is the Expo-recommended setup
# for pnpm monorepos and is required for SDK 54's Metro to resolve.
node-linker=hoisted
