{
  "name": "@swap/server",
  "version": "0.1.0",
  "private": true,
  "description": "Shared server-side utilities for SWAP!: auth, authorization, typed errors, SQLSTATE mapping, retry, validation, rate limiting, audit, and Supabase client factories. Server-only — never bundle the service-role client into a browser/mobile app.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "@swap/types": "workspace:*",
    "@swap/validation": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
